# township-cli-example

Example CLI for [township-client](https://github.com/joehand/township-client).

## Usage

* Run `npm start` to start a local township server
* Run `node cli.js <cmd>` to try basic commands